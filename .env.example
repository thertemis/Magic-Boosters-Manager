# PostgreSQL credentials
POSTGRES_USER=mtg_user
POSTGRES_PASSWORD=change_me_to_a_strong_password
POSTGRES_DB=mtg_pack_sim

# App session secret (use a long random string)
SESSION_SECRET=change_me_to_a_random_secret

# Port the app is exposed on (default 5000)
APP_PORT=5000
